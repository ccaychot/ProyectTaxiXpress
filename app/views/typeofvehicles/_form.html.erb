<%= form_for(@typeofvehicle) do |f| %>
  <% if @typeofvehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typeofvehicle.errors.count, "error") %> prohibited this typeofvehicle from being saved:</h2>

      <ul>
      <% @typeofvehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
